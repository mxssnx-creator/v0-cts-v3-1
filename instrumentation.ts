/**
 * Startup initialization - deferred to first use in production only
 */
export async function register() {
  // This is a no-op in dev/preview
  // Production initialization happens on first database access
}
